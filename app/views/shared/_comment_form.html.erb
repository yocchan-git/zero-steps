<div class="d-flex justify-content-center">
  <div class="col-lg-6 col-md-8 col-12">
    <%= form_with(model: [ commentable, Comment.new ]) do |form| %>
      <div class="mb-3">
        <%= form.label :content, "コメント(500文字以内)", class: 'form-label' %>
        <%= form.text_area :content, class: 'form-control', required: true, maxlength: 500 %>
      </div>

      <div class="d-flex justify-content-center">
        <%= form.submit 'コメントする', class: 'btn btn-primary me-3' %>
      </div>
    <% end %>
  </div>
</div>
