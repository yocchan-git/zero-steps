<%# TODO: ヘルパーメソッドにリファクタする %>
<% request_uri = reactionable_uri(@reaction) %>

<%= turbo_stream.update "#{@reaction.reactionable_id_name}" do %>
  <%= render partial: "shared/unreactions", locals: {reactionable: @reaction.reactionable, request_uri:} %>
<% end %>
