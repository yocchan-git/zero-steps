<% if current_user != @user %>
  <div class="follow_form_div">
    <div id="follow_form">
    <% if current_user.following?(@user) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
    </div>
  </div>
<% end %>
