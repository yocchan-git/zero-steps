<% if current_user != user %>
  <div id="follow_form<%= user.id %>">
  <% if current_user.following?(user) %>
    <%= render 'unfollow', user:, is_users_page: %>
  <% else %>
    <%= render 'follow', user:, is_users_page: %>
  <% end %>
  </div>
<% end %>
